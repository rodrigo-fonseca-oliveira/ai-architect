name: cmdline_pytest_quiet
type: cmdline
command: ./.venv/bin/pytest
parameters:
  args:
    - -q
    - tests
working_dir: .
env:
  - key: VIRTUAL_ENV
    value: .venv
  - key: PATH
    value: .venv/bin:${PATH}
