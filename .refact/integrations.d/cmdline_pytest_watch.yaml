name: cmdline_pytest_watch
type: cmdline
command: ./.venv/bin/ptw
parameters:
  args:
    - --onfail
    - notify-send
    - tests
working_dir: .
env:
  - key: VIRTUAL_ENV
    value: .venv
  - key: PATH
    value: .venv/bin:${PATH}
